const projects = []; 


const newProject = {title: "title", todos:[]} 


function createProject(title){
    title = tile; 
    tasks = [];

    projects.push({ title, tasks });
} 


function addTask(project, task){
    project.todo.push(task);
} 

function getProjects(){
    return projects;
} 

